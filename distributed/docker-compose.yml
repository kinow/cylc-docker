version: '3'

services:
  cylc-ssh:
    build: .
    environment:
      - CYLC_SSH_PUBKEY
    volumes:
      - ./cylc:/opt/cylc
    ports:
      - 22
  cylc-jump-box:
    build: .
    environment:
      - CYLC_SSH_PUBKEY
    volumes:
      - ./cylc:/opt/cylc
